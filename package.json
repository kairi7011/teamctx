{
  "name": "teamctx",
  "version": "0.1.0",
  "description": "Git-backed team context layer for AI coding tools.",
  "type": "module",
  "bin": {
    "teamctx": "./dist/cli/index.js",
    "teamctx-mcp": "./dist/mcp/server.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "test": "tsc -p tsconfig.test.json && node --experimental-test-isolation=none --test \"dist-test/test/**/*.test.js\"",
    "smoke:install": "node scripts/install-smoke.mjs",
    "ci": "npm run format:check && npm run lint && npm run check && npm run test && npm run build && npm run smoke:install"
  },
  "keywords": [
    "mcp",
    "ai",
    "coding-agent",
    "claude-code",
    "codex",
    "context"
  ],
  "author": "kairi7011",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
